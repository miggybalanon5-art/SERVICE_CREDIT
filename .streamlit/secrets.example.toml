# Copy these keys into Streamlit Community Cloud secrets or a local
# .streamlit/secrets.toml file. Do not commit real authorization codes.
ADMIN_AUTH_CODE = "replace-with-a-long-random-admin-code"
EMPLOYEE_AUTH_CODE = "replace"
